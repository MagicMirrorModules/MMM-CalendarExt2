const config = {
  "overrides": [
    {
      "files": "*.md",
      "options": {
        "parser": "markdown",
        "embeddedLanguageFormatting": "off"
      }
    }
  ],
  "trailingComma": "none"
};

export default config;
